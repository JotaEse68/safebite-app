[build]
  publish = "."
  functions = "functions"

[functions]
  node_bundler = "esbuild"

[functions."analyze"]
  timeout = 26

# SPA fallback - solo esto, Netlify maneja /.netlify/* automáticamente
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
